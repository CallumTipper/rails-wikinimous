<div class="back">
  <%= link_to "Back", articles_path %>
</div>

<div class="show-article">
  <h2><%= @article.title %></h2>
  <p><%= @article.content %></p>
  <div class="buttons">
    <%= link_to "Edit", edit_article_path(@article.id) %>
    <%= link_to "Delete", article_path(@article.id), method: :delete,data: { confirm: "Delete?" } %>
  </div>
</div>
